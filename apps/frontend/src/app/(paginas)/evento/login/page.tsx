"use-client"
// import FormLoginAdmin from "@/components/evento/FormLoginAdmin"
// import useAPI from "@/data/hooks/useAPI"
// import { use, useCallback, useEffect, useState } from "react";



export default function PaginaLogin(props: any) {
  // const { httpPost } = useAPI();
  // const params: any = use(props.params);
  // const [senha, setSenha] = useState<string>(params.todos[1] ?? "");
  // const [email, setEmail] = useState<string>(params.todos[1] ?? "");

  // const obterEventos = useCallback(async () => {
  //   if (!id || !senha) return;
  //   const evento = await httpPost("/eventos", { id, senha });
  //   console.log({ evento })
  //   setEvento(evento);
  // }, [httpPost, id, senha]);

  return (
    <div><h2>Coming soon...</h2></div>
    // <FormLoginAdmin />
  )
}
